fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
PyJWT>=2.8
opencv-python>=4.10
numpy>=1.26
httpx2>=2.12                 # drop-in replacement; silences starlette.testclient deprecation
reportlab>=4.0  # court-style Vehicle Movement Evidence Report PDF
# Optional. Only needed when ALERT_BUS_URL is set, to fan alerts across more
# than one central instance. Without it the in-process socket fanout is used
# and the platform logs why.
redis>=5.0
