# python
backend/.venv/
venv/
__pycache__/
*.pyc

# secrets and local state
backend/.env
backend/sentinel.db
backend/snapshots/*
!backend/snapshots/.gitkeep

# model weights — large, ship with the deploy not the repo
backend/models/*.onnx
backend/models/*.pt

# node
frontend/node_modules/
frontend/dist/

# local media and bench output
data/samples/*.mp4
data/samples/*.mkv
bench/results/*.json

# edge spool (runtime state)
backend/spool/

# python tooling
.pytest_cache/
